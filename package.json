{
  "name": "ios-calculator",
  "version": "1.0.0",
  "description": "iOS Calculator Clone — точная копия калькулятора Apple для Android",
  "main": "index.html",
  "scripts": {
    "start": "npx serve .",
    "build:android": "npx cap sync android && cd android && ./gradlew assembleDebug",
    "open:android": "npx cap open android",
    "sync": "npx cap sync"
  },
  "dependencies": {
    "@capacitor/android": "^5.7.0",
    "@capacitor/core": "^5.7.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.7.0",
    "serve": "^14.2.0"
  },
  "keywords": ["calculator", "ios", "pwa", "capacitor", "android"],
  "author": "",
  "license": "MIT"
}
